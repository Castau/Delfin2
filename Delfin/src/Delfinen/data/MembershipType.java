package Delfinen.data;

/**
 * MembershipType relates to price. Class handles whether a user is passive (price: 500dkk/year) or active (price depends on age).
 * @author runin, asger og camilla
 */
public enum MembershipType
{
    ACTIVE, PASSIVE;
}

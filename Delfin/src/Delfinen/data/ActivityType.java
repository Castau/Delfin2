package Delfinen.data;

/**
 *
 * @author runin, camilla og asger
 * Contains the activity types relevant to active memberships.
 * If a member is marked as BASIC, they do not compete.
 * If a member is marked as COMPETITIVE, they do compete.
 */
public enum ActivityType
{
    COMPETITIVE, BASIC;
}
